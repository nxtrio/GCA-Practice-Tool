{
  "name": "@gca-practice/assessment-schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./assessment.schema.json": "./assessment.schema.json"
  },
  "files": [
    "assessment.schema.json",
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.tests.json --pretty false"
  },
  "dependencies": {
    "@gca-practice/contracts": "0.0.0",
    "@gca-practice/runner-core": "0.0.0",
    "ajv": "8.20.0"
  }
}
