name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - run: npm ci
      - run: npm run typecheck
      # Native runner suites compile and execute Java/C++ programs. Running test
      # files concurrently can starve toolchain detection on small hosted runners.
      - run: npm test -- --no-file-parallelism
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
